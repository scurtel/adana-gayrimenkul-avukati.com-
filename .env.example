# Site / runtime (Hostinger Node.js)
HOST=0.0.0.0
PORT=4321

# Optional analytics / forms / Search Console (src/config/site.ts)
# Leave empty in Hostinger if unused; values are configured in code/config for this project.

# Content generation only (local scripts — not required for production start)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_GOOGLE_SEARCH_ENABLED=true
